import ToggleTheme from "./ToggleTheme";

const Navbar = () => {
    return (
        <header>
            <h1>Job Tracker</h1>
            <ToggleTheme />
        </header>
    );
};

export default Navbar;
